# ##################################################
# ## Components/Sensors
# ##################################################
input_boolean:
  low_salt:
    name: Low Salt
    icon: mdi:hydraulic-oil-level

# ##################################################
# ## Recorder/History
# ##################################################
recorder:
  include:
    entities:
      - binary_sensor.garage_monitor_motion
      - sensor.garage_monitor_water_temperature
      - sensor.garage_monitor_salt_level

history:
  include:
    entities:
      - binary_sensor.garage_monitor_motion
      - sensor.garage_monitor_water_temperature
      - sensor.garage_monitor_salt_level
